---
name: activate
description: Initialize Demo Maker for the current project. Creates .demo-maker/ directory, config file, and prompts for API keys.
---

Run the `activate-demo-maker` skill workflow.

Create `.demo-maker/` with `config.json`, `captures/`, `narration/`, and `clips/`. Add `.demo-maker/` to `.gitignore`. Run preflight checks and confirm activation.
